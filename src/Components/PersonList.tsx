import React from 'react';
import {Data} from './Interfaces';

interface PList{
    items : Data[]
}

const PersonList: React.FC<PList> =(Props : Array<object>) => {
    return (
        <div>
            
        </div>
    )
}

export default PersonList;